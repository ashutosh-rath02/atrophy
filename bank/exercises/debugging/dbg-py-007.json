{
  "id": "dbg-py-007",
  "kind": "fix",
  "axis": "debugging",
  "language": "python",
  "tier": 3,
  "title": "Slug counts grow garbage keys",
  "prompt": "count_tag_slugs(tags) should normalize each tag into a slug, then return a dict of slug -> count.\nLowercase it, trim it, turn any run of non-alphanumeric separators into one '-',\nthen strip leading/trailing '-'. Ignore None or tags that normalize to empty.\nGarbage separators still leak into the keys. Find and fix the bug.",
  "functionName": "count_tag_slugs",
  "starterCode": "def count_tag_slugs(tags):\n    counts = {}\n    for tag in tags:\n        if tag is None:\n            continue\n        slug = tag.strip().lower().replace(\" \", \"-\")\n        counts[slug] = counts.get(slug, 0) + 1\n    return counts\n",
  "softTimeLimitSeconds": 600,
  "tests": [
    { "args": [[" Data Science ", "data-science", "DATA_science", "data/science!!"]], "expected": { "data-science": 4 } },
    { "args": [[null, "   ", "!!!", "---", "__"]], "expected": {} },
    { "args": [["ship_it", "ship it", "ship---it"]], "expected": { "ship-it": 3 } },
    { "args": [[]], "expected": {} },
    { "args": [["Already-Clean"]], "expected": { "already-clean": 1 } }
  ]
}
