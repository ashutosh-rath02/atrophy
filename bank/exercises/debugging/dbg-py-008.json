{
  "id": "dbg-py-008",
  "kind": "fix",
  "axis": "debugging",
  "language": "python",
  "tier": 3,
  "title": "The parser trusts every row",
  "prompt": "parse_score_rows(lines) should parse valid \"name,score\" rows into [name, score] pairs,\nin input order. Trim both fields, keep only rows with a non-empty name and an integer score,\nand ignore blank or malformed lines. One bad row still takes the whole thing down.\nFind and fix the bug.",
  "functionName": "parse_score_rows",
  "starterCode": "def parse_score_rows(lines):\n    rows = []\n    for line in lines:\n        name, score = line.split(\",\")\n        rows.append([name.strip(), int(score)])\n    return rows\n",
  "softTimeLimitSeconds": 600,
  "tests": [
    { "args": [["alice,10", " bob , -2 ", "carol,0"]], "expected": [["alice", 10], ["bob", -2], ["carol", 0]] },
    { "args": [["", "alice,10", "bad", "bob, x", "carol,3,extra", " ,4", "dave,5"]], "expected": [["alice", 10], ["dave", 5]] },
    { "args": [[]], "expected": [] },
    { "args": [["eve, 7", " frank , 8 "]], "expected": [["eve", 7], ["frank", 8]] },
    { "args": [["   ", "zoe,1"]], "expected": [["zoe", 1]] }
  ]
}
